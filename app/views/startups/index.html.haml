%h1 Startups
= link_to 'New Startup', new_startup_path

%br 

%table
  %tbody
    - @startups.each do |startup|
      %tr
        %td= link_to (image_tag startup.image.url(:thumb)), startup
        %td= link_to startup.name, startup       

%br

